# Index Freshness

Index freshness measures whether the retrieval index reflects the latest source documents. A stale index can return outdated answers even when the document folder is current. Production systems should track document checksums, ingestion timestamps, and index version identifiers.

