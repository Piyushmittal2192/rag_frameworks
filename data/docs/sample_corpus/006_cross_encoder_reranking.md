# Cross Encoder Reranking

A cross encoder reranker scores query and chunk pairs directly. It is usually applied after vector and BM25 retrieval to reorder the candidate set before sending context to the LLM.
