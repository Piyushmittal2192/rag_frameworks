# Standard RAG Overview

Standard retrieval augmented generation retrieves relevant chunks from an indexed knowledge base and passes them to an LLM. The LLM should answer using only the retrieved context and cite the source chunks.
