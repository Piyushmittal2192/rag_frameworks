# Corrective RAG Overview

Corrective RAG adds quality checks to retrieval. It grades retrieved context, rewrites weak queries, retrieves again, and only then generates an answer from the corrected context.
