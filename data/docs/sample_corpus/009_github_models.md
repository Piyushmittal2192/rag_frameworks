# GitHub Models

GitHub Models provides a hosted chat-completions interface. The framework uses the github-models provider with a GitHub token, a model ID from the GitHub Models catalog, and the models inference endpoint.
