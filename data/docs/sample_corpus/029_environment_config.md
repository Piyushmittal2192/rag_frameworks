# Environment Configuration

Configuration is loaded from environment variables or a local .env file. Important settings include LLM_PROVIDER, LLM_MODEL, GITHUB_TOKEN, TOP_K, ENABLE_RERANKER, and RERANKER_MODEL.
